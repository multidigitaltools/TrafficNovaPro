<Window x:Class="TrafficNova.Dialogs.ProxyExportDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export Proxies"
        Width="400" Height="280"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Export Proxies" Style="{StaticResource HeadingSmall}" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Style="{StaticResource CardStyle}">
            <StackPanel>
                <TextBlock Text="Export format:" Style="{StaticResource BodySecondary}" Margin="0,0,0,8"/>
                <RadioButton x:Name="FormatHostPort" Content="host:port" IsChecked="True" Margin="0,0,0,6"/>
                <RadioButton x:Name="FormatFull" Content="protocol://user:pass@host:port" Margin="0,0,0,6"/>
                <RadioButton x:Name="FormatColons" Content="host:port:user:pass" Margin="0,0,0,16"/>
                <TextBlock Text="Scope:" Style="{StaticResource BodySecondary}" Margin="0,0,0,8"/>
                <RadioButton x:Name="ScopeAll" Content="All proxies" IsChecked="True" Margin="0,0,0,6"/>
                <RadioButton x:Name="ScopeVisible" Content="Visible (current filter)" Margin="0,0,0,0"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource SecondaryButtonStyle}"
                    Width="90" Margin="0,0,12,0" Click="Cancel_Click"/>
            <Button Content="Export…" Style="{StaticResource PrimaryButtonStyle}"
                    Width="90" Click="Export_Click"/>
        </StackPanel>
    </Grid>
</Window>

<UserControl x:Class="TrafficNova.Pages.LogsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{DynamicResource BackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Text="Logs" Style="{StaticResource HeadingLarge}" VerticalAlignment="Center"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button Content="↻ Refresh" Style="{StaticResource ToolbarButtonStyle}"
                        Command="{Binding LoadLogsCommand}" Margin="0,0,8,0"/>
                <Button Content="Clear" Style="{StaticResource ToolbarButtonStyle}"
                        Command="{Binding ClearLogsCommand}" Margin="0,0,8,0"/>
                <Button Content="Open Log Folder" Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding OpenLogFolderCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Filters -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="0,0,0,16">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Level:" VerticalAlignment="Center" Margin="0,0,8,0"
                           Style="{StaticResource BodySecondary}"/>
                <ComboBox ItemsSource="{Binding LevelOptions}"
                          SelectedItem="{Binding FilterLevel}"
                          Width="140" Margin="0,0,16,0"/>
                <TextBlock Text="Search:" VerticalAlignment="Center" Margin="0,0,8,0"
                           Style="{StaticResource BodySecondary}"/>
                <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                         Width="240" Margin="0,0,16,0"/>
                <CheckBox Content="Auto-refresh (5s)" IsChecked="{Binding AutoRefresh}"
                          VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Log DataGrid -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Padding="0">
            <DataGrid ItemsSource="{Binding Entries}" IsReadOnly="True"
                      VirtualizingPanel.IsVirtualizing="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Time" Binding="{Binding Timestamp, StringFormat=HH:mm:ss.fff}" Width="100"/>
                    <DataGridTemplateColumn Header="Level" Width="90">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Padding="4,2" CornerRadius="3" HorizontalAlignment="Left">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Information">
                                                    <Setter Property="Background" Value="{DynamicResource LogInfoBadgeBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                    <Setter Property="Background" Value="{DynamicResource LogWarningBadgeBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Background" Value="{DynamicResource LogErrorBadgeBrush}"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Fatal">
                                                    <Setter Property="Background" Value="{DynamicResource LogErrorBadgeBrush}"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Level}" FontSize="11" FontWeight="SemiBold"/>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Message" Binding="{Binding Message}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

    </Grid>
</UserControl>
